{
  "name": "goldenage-ai",
  "version": "1.0.0",
  "private": true,
  "description": "GoldenAge AI — senior-friendly AI companion + guardian ecosystem (static web app hosted on Vercel).",
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "vercel-build": "echo 'Vercel static build'",
    "dev": "echo 'Open index.html in a browser, or run: npx serve .'",
    "check:syntax": "node --check app.js && node --check app-live.js && echo 'All JS files OK'",
    "check:health": "curl -s -o /dev/null -w 'llm-chat: %{http_code}\\n' https://exvlolipycabnqiaptib.supabase.co/functions/v1/llm-chat",
    "check": "npm run check:syntax && npm run check:health"
  },
  "engines": {
    "node": ">=18"
  },
  "//": [
    "This file is here so Vercel recognises the project as a Node-capable",
    "package and activates Edge Middleware (middleware.ts at root).",
    "Without it, Vercel treats the project as pure HTML and ignores the",
    "middleware. There are no runtime dependencies — the static files in",
    "this repo are served as-is, and middleware.ts runs at the edge."
  ]
}
